% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slice_intensity.R
\name{slice_intensity}
\alias{slice_intensity}
\title{Append intensity values to slice_axis data frame}
\usage{
slice_intensity(file, df_slice_axis = NULL)
}
\arguments{
\item{file}{Path to an image file.}

\item{df_slice_axis}{Data frame containing voxel coordinates with columns: i, j, k (voxel indices) and x, y, z (world coordinates). Typically output from slice_axis() function. Default is NULL.}
}
\value{
A data frame with columns: intensity, x, y, z, i, j, k.
}
\description{
Append intensity values to slice_axis data frame
}
\examples{
\dontrun{
# First get coordinates from slice_axis
coords <- slice_axis("brain_image.nii", "y", c(7.5, 88))

# Then extract intensity values at those coordinates
result <- slice_intensity("brain_image.nii", coords)}
}
